# Dependencies for the LIVE pipeline (provisioning, recreation, DB/cache).
#
#   pip install -r requirements-live.txt
#
# Claude VCS/triage/watch enhancement (Black Duck + GitHub REST use urllib/stdlib).
anthropic>=0.40

# Durable seeds (Postgres) + recreatable cache (Redis) backing the recreate pipeline.
# Postgres/Redis run locally in WSL2 (see infra/stack.sh); the app connects over
# localhost. psycopg[binary] bundles libpq so nothing extra is needed on Windows.
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
redis>=5.0
